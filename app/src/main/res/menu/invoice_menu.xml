<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_view_history"
        android:icon="@android:drawable/ic_menu_recent_history"
        android:title="@string/view_history"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/action_edit_last_invoice"
        android:icon="@android:drawable/ic_menu_edit"
        android:title="@string/edit_last_invoice"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/action_delete_last_invoice"
        android:icon="@android:drawable/ic_menu_delete"
        android:title="@string/delete_last_invoice"
        app:showAsAction="ifRoom" />
</menu>
